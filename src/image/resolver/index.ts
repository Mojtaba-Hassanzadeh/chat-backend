import { ImageMutationResolver } from './image-mutation.resolver';
import { ImageQueryResolver } from './image-query.resolver';

export const ImageResolvers = [ImageMutationResolver, ImageQueryResolver];
